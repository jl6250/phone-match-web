# 复制为 scripts/deploy.env 后填写真实令牌（deploy.env 已被 .gitignore 忽略，勿提交）。
# deploy.sh 在拉取前会 source 本文件，用它设置带令牌的 origin 远程，实现免交互拉取。

# Codeup 用户名（格式：<AliyunUID>_<企业>）
GIT_REMOTE_USER=239137892954118917_digiplus

# 个人访问令牌（pt- 开头），在 个人设置 → 访问令牌 创建
GIT_REMOTE_TOKEN=pt-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
